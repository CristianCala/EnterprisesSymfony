name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  getCommentOnIssue:
    runs-on: ubuntu-latest
    steps:
      - name: Get Comment
        uses: actions/github-script@v3
        with:
          script: |
            const issueNumber = context.payload.pull_request.base
            console.log(issueNumber)
