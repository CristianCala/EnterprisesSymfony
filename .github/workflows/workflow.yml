name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  update-todo:
    runs-on: ubuntu-20.04
    steps:
      - uses: ikanago/issue-deadline-manager@v1
